<!DOCTYPE html>
<html>
  <head>
    <title>Checkers</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-uWxY/CJNBR+1zjPWmfnSnVxwRheevXITnMqoEIeG1LJrdI0GlVs/9cVSyPYXdcSF"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="about.css" />
    <link rel="stylesheet" href="index.css" />
    <link rel="stylesheet" href="leaderboard.css" />
  </head>
  <body>
    <style>
      span {
        font-size: 20px;
        padding-left: 20px;
        font-family: "Nunito", sans-serif;
      }
    </style>
    <div style="isolation: isolate" class="about-section">
      <h2 style="font-family: courgette">My profile</h2>
    </div>
    <div class="container" style="display: flex; justify-content: center">
      <div class="div1" style="margin-top: 100px; margin-left: 150px">
        <span style="display: block"
          ><b>Name :</b> <span> <%=data[0].name%></span></span
        >
        <span style="display: block"
          ><b>Email :</b> <span> <%=data[0].email%></span></span
        >
        <span style="display: block"
          ><b>Highest Score : </b> <span><%= data[0].score%></span></span
        >
        <span style="display: block"
          ><b>Total games played : </b>
          <span><%= data[0].no_of_games%></span></span
        >
        <span style="display: block"
          ><b>Wins : </b> <span id="wins"><%= data[0].wins%></span></span
        >
        <span style="display: block">
          <b>Losses : </b>
          <span><%= data[0].no_of_games - data[0].wins %></span>
        </span>
        <span style="display: block">
          <b>Accuracy : </b>
          <span><%=data[0].wins / data[0].no_of_games *100 %></span>
        </span>
        <span style="display: block"
          ><b>Best Win Time :</b> <span><%= data[0].best_win_time%></span></span
        >
      </div>
      <div class="div2" style="margin-left: -18px">
        <span id="piechart" style="margin: auto; margin-left: 100px"></span>
      </div>
    </div>
    <canvas id="myChart" style="isolation: isolate; width: 100%; max-width: 700px"></canvas>
    <!-- <div class="col"> -->
    <!-- </div> -->
    <script
      type="text/javascript"
      src="https://www.gstatic.com/charts/loader.js"
    ></script>
    
    <script type="text/javascript">
      function htmlDecode(input) {
        var e = document.createElement("div");
        e.innerHTML = input;
        return e.childNodes.length === 0 ? "" : e.childNodes[0].nodeValue;
      }
      var myData = JSON.parse(htmlDecode("<%= JSON.stringify(data) %>"));
      console.log(myData[0].score_per_month);
      // Load google charts
      google.charts.load("current", { packages: ["corechart"] });
      google.charts.setOnLoadCallback(drawChart);

      // Draw the chart and set the chart values
      function drawChart() {
        var dataPie = google.visualization.arrayToDataTable([
          ["Task", "Hours per Day"],
          ["Win", parseInt(document.getElementById("wins").innerHTML)],
          ["Lose", 5],
        ]);

        // Optional; add a title and set the width and height of the chart
        var options = {
          title: "My Average Performance",
          width: 550,
          height: 400,
        };

        // Display the chart inside the <div> element with id="piechart"
        var chart = new google.visualization.PieChart(
          document.getElementById("piechart")
        );
        chart.draw(dataPie, options);
      }

      //BAR GRAPH

      var xValues = [
        "Jan",
        "Feb",
        "March",
        "April",
        "May",
        "June",
        "July",
        "Aug",
        "Sep",
        "Oct",
        "Nov",
        "Dec",
      ];
      yValues = [];
      for (let i = 0; i < 12; i++) {
        yValues[i] = parseInt(myData[0].score_per_month[i]);
      }

      var barColors = "#614fc7";

      new Chart("myChart", {
        type: "bar",
        data: {
          labels: xValues,
          datasets: [
            {
              backgroundColor: barColors,
              data: yValues,
            },
          ],
        },
        options: {
          legend: { display: false },
          title: {
            display: true,
            text: "Score Per Month Analysis",
            // fontColor: red, 
            // fontName: "Courier New",    // 'Times New Roman'
            fontSize: 20,               // 12, 18
          },
          
          
        },
      });

      // var canvas = document.querySelector("#myChart");
      // var context = canvas.getContext("2d");
      // context.font = "bold 96px Helvetica, Arial, sans-serif";
      // context.fillStyle = "steelblue";
      // context.strokeStyle = "#173b79";

    </script>
    <script
      language="javascript"
      type="text/javascript"
      src="index.js"
    ></script>
    <script
      language="javascript"
      type="text/javascript"
      src="../routes/leaderboard.js"
    ></script>
    <style>
      /* #myChart{
        color: aqua;
      } */
     
      rect {
        width: 0px;
        height: 0px;
      }
      svg {
        margin-left: 80px;
      }
      svg g text {
        font-size: 18px;
        fill: rgb(100, 201, 41);
      }
    </style>
  </body>
</html>
